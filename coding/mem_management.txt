内存管理、分配

1.	堆、栈

	a.	堆（heap)

		1.	堆缺乏组织

			a.	可用于存储在编译时未知大小、大小可能变化的数据

			b.	操作系统在堆上分配空间，并返回指向改位置的pointer

			c.	而pointer大小固定，可以直接存储在栈上

	b.	栈（stack）

		1.	栈操作迅速

			a.	不需要寻址，因为位置总是在栈顶

			b.	所有数据大小都是已知、固定
